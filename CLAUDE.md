# CLAUDE.md — 졸업과제 에이전트 (13_졸업과제)

이 폴더는 동아대학교 졸업논문 프로젝트의 작업 공간이자 **깃허브 공개 저장소**입니다.
세션 시작 시 이 파일을 먼저 읽고, 보관함 최상위 `../CLAUDE.md`(글쓰기 톤·공통 금지사항)도 따릅니다.

## 이 프로젝트가 하는 일 (한 줄)

**"행동에 비용이 붙는 환경에서, '언제 행동할지'를 배우는 강화학습이 단순 고정 규칙보다 나아지는 조건(비용 λ)을 실험으로 찾는다."**

- 비교 대상: (가) 표준 DQN (나) TempoRL 방식(행동 지속 길이 학습) (다) Lazy-MDP 방식(개입할 상태 선택) (라) 고정 규칙(k스텝 주기 행동, 임계값 규칙, 무행동)
- 환경: Gymnasium MountainCar-v0, LunarLander-v2(안 되면 v3), MinAtar. 보상을 `r' = r − λ·1[행동 실행]`으로 수정
- 최종 산출물: ① λ-성능 지도 ② "학습이 규칙을 이기는 임계 비용" 표 ③ 졸업논문 ④ 잘 관리된 깃허브 저장소
- 상세 설계: `docs/02_실험-설계.md` / 일정: `docs/03_로드맵.md` / 계획서 원문 요약: `docs/01_계획서-요약.md`

## 절대 규칙 (연구 무결성 — 예외 없음)

1. **시드 10개 이상 + IQM(사분위평균) + 계층 부트스트랩 신뢰구간**으로만 결론을 말한다 (Agarwal et al. 2021 프로토콜). 소수 시드 평균으로 "이겼다/졌다" 선언 금지 → `rliable-reporting` 스킬
2. **공정 비교**: 모든 에이전트·규칙은 같은 환경 스텝 예산, 같은 λ, 같은 평가 방식(탐험 끔)으로 비교한다
3. **재현성**: 모든 실험은 config 파일(시드 목록 포함)로 실행하고, 결과 옆에 config 사본을 저장한다. "손으로 한 번 돌려본 결과"는 논문에 못 쓴다 → `rl-experiment-protocol` 스킬
4. **나쁜 결과도 기록한다.** 기대와 다른 결과는 지우는 게 아니라 논문의 '논의' 재료다. 숫자는 절대 만들어내지 않고, 항상 `results/` 로그 파일에서만 인용한다
5. **기준선 먼저.** 고정 규칙(k스텝, 임계값, 무행동)과 표준 DQN이 먼저 완성·검증된 뒤에 TempoRL/Lazy-MDP로 간다
6. 검증(테스트) 결과를 보고 하이퍼파라미터를 다시 고치는 행위 금지 — 튜닝은 별도 파일럿에서만

## 워크플로 규칙

- **긴 작업 전 예상 소요시간을 먼저 한글로 안내**한다 (예: "λ 6개 × 시드 10개 × 3환경 ≈ CPU로 약 N시간"). TempoRL 원논문 기준 CPU 1코어 15시간 이내 규모 — 개인 PC로 가능하지만 밤샘 실험은 체크포인트 필수
- 긴 실험은 **체크포인트 저장 + 이어하기** 가능하게 만들고, 중간 진행 상황을 한글로 보고한다
- 실험이 끝나면 HTML 보고서를 `results/reports/`에 만들고 **Microsoft Edge로 자동 오픈**한다:
  `start msedge "결과파일의 전체경로.html"`
- 실험 결과 보고에는 항상: 총보상(IQM+신뢰구간), 행동 횟수, 학습 곡선, 고정 규칙 대비 우열, 실험 조건(λ, 시드 수, 스텝 예산)을 포함
- **세션을 마칠 때마다**: ① 의미 있는 git 커밋 ② README 진행 상황 갱신 ③ 새로 알게 된 것은 옵시디언 위키(`../2_위키/`)에 남긴다 → `github-project-log` 스킬
- 새 실험 전에 `docs/실험일지.md`(없으면 생성)에서 이미 해본 조건인지 확인한다

## 깃허브 규칙

이 저장소는 **채용 담당자가 보는 공개 포트폴리오**다. 커밋 메시지·이슈·README 관리 방법은 `github-project-log` 스킬을 따른다.
`results/raw/`의 대용량 파일과 모델 체크포인트는 커밋하지 않는다 (.gitignore 확인).

## 스킬 (이 폴더 `.claude/skills/`)

| 스킬 | 언제 쓰나 |
|---|---|
| rl-experiment-protocol | 실험을 설계·실행할 때 (config, 시드, 체크포인트, 파일럿) |
| rliable-reporting | 결과를 집계·그림으로 만들 때 (IQM, 신뢰구간, λ-성능 지도) |
| thesis-writing | 논문·보고서 원고를 쓸 때 |
| github-project-log | 작업을 마무리하고 커밋·이슈를 정리할 때 |

## 금지 사항

- `../1_원본자료/` 수정·삭제 금지 (읽기만)
- API 키·비밀번호를 코드/문서에 적지 않기 (필요하면 `.env`)
- 확인 안 한 것을 확인한 것처럼 쓰지 않기 — 모르면 "확인 필요"
- 결과 그림/표를 논문에 넣을 때 출처 로그 파일 경로를 주석으로 남기지 않는 것

## 참고문헌 (논문에 기재된 3편)

1. Biedenkapp et al., "TempoRL: Learning When to Act", ICML 2021 (공개 코드: automl/TempoRL)
2. Jacq et al., "Lazy-MDPs: Towards Interpretable RL by Learning When to Act", AAMAS 2022
3. Agarwal et al., "Deep RL at the Edge of the Statistical Precipice", NeurIPS 2021 (평가 프로토콜, 라이브러리: rliable)
